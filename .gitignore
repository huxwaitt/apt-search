# ── Personal / private (never commit) ──
# config.local.json holds your home base, commute, ZIP
config.local.json
# generated reports contain listings + your commute
apartments_*.html
*.html
# ...except the curated design showcase (neutral demo data)
!docs/sample.html
# ...and the bookmarklet install pages (part of the tool, no private data)
!zillow-bookmarklet.html
!apartments-bookmarklet.html

# per-run output folders (reports, copied dumps, run.json with your criteria/commute)
data/

# ── Secrets ──
.env
*.key
gmaps_key*.txt

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# ── Scratch / temp test files ──
_*.py
*.log

# ── OS / editor ──
.DS_Store
Thumbs.db
.vscode/
.idea/
